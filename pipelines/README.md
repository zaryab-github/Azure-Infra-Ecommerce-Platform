# pipelines — not yet implemented

Populated in **Phase 10 — CI/CD**. Will hold `ci.yml` (build → unit test → docker build → push ACR) and `cd.yml` (terraform plan/apply → deploy to AKS → smoke test) as Azure Pipelines YAML, per the flow in [`docs/ROADMAP.md`](../docs/ROADMAP.md).
