# kubernetes/hpa — not yet implemented

Populated in **Phase 5 — AKS**. Will hold a `HorizontalPodAutoscaler` per service, scaling on CPU (and later, custom metrics from Application Insights once Phase 11 lands).
