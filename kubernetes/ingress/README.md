# kubernetes/ingress — not yet implemented

Populated in **Phase 5 — AKS**. Will hold the ingress controller install (NGINX or AGIC — Application Gateway Ingress Controller, to reuse the App Gateway WAF from the architecture diagram) and `Ingress` resources routing to the three services.
