# helm — optional, not yet implemented

Optional packaging layer for the manifests in [`kubernetes/`](../kubernetes), and a prerequisite for **Optional Phase 13 — GitOps** (FluxCD/ArgoCD watching a chart repo). Not required for the core Phase 1–12 build.
