# Azure Cache for Redis — not yet implemented

Optional, alongside **Phase 6** (see the architecture diagram in [`docs/ROADMAP.md`](../ROADMAP.md)).

**What it will be**: a managed Redis cache, used for something like product-catalog read caching or session state — the specific use case will be decided when this phase is picked up, since it's not a hard requirement of any single roadmap phase.

**Why it's here at all**: it's in the architecture diagram alongside Azure SQL, demonstrating a caching layer distinct from the system of record.

This file will be filled in once this is built.
