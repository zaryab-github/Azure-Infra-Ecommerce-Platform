# aks module — not yet implemented

Built in **Phase 5 — AKS** (see [`docs/ROADMAP.md`](../../../docs/ROADMAP.md)).

Will provision: AKS cluster, system + user node pools, ingress controller wiring, HPA prerequisites, integration with the `network` module's `snet-aks` subnet and the `identity` module's managed identity.
