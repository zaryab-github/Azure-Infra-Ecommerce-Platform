# sql module — not yet implemented

Built in **Phase 6 — Databases** (see [`docs/ROADMAP.md`](../../../docs/ROADMAP.md)).

Will provision: Azure SQL logical server + database (Basic tier), firewall/private-endpoint wiring into `snet-data`, and the `users` / `orders` / `products` schema. Cosmos DB is a later, optional addition.
