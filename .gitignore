# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Node
node_modules/
npm-debug.log*
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Kubernetes local kubeconfigs
kubeconfig
*.kubeconfig

# Kubernetes Secret manifests with real values filled in — never commit these.
# Only the *.example.yaml templates are tracked.
kubernetes/secrets/app-secrets.yaml
